version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.11
    commands:
      - pip install -r requirements.txt

  build:
    commands:
      - python mlops_l1/mlops_l1.py

artifacts:
  files:
    - mlops_l1/telecom_tower_failure_model.pkl
